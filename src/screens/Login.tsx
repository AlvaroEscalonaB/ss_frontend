const Login = () => {
  return (
    <main>
      <form>
        <section>
          <label>Nombre</label>
          <input />
        </section>
        <button onClick={() => console.log(3)}> Login </button>
      </form>
    </main>
  );
};

export default Login;
